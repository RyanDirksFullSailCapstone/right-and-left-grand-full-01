using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DancerTargets : MonoBehaviour
{
    public GameObject ForwardSpaceTarget;
    public GameObject BackwardSpaceTarget;
    public GameObject RightSpaceTarget;
    public GameObject LeftSpaceTarget;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
